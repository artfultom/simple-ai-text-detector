[project]
name = "mlops-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fire>=0.7.1",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.0",
]
